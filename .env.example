# Frontend Environment Variables
# Only REACT_APP_* variables are accessible in React

# API Configuration
REACT_APP_API_BASE_URL=http://localhost:8000

# Optional: Feature Flags (uncomment to use)
# REACT_APP_ENABLE_MOCK_DATA=false
# REACT_APP_DEBUG_MODE=false

# Note: Backend variables (DATABASE_URL, JWT_SECRET_KEY, etc.) 
# should be in a separate backend/.env file
